spring:
  application:
    name: recruit_platform
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/main_db
    username: admin
    password: secret
